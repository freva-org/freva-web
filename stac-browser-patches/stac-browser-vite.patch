--- a/vite.config.js
+++ b/vite.config.js
@@ -81,6 +81,7 @@ export default defineConfig(async ({ mode }) => {
     build: {
+      manifest: true,
       sourcemap: mode !== "minimal",
       rollupOptions: {
         external: ["fs/promises"],
       },
     },